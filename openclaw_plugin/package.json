{
  "name": "memory-palace",
  "version": "2.0.1",
  "description": "OpenClaw native tool plugin for Memory Palace — direct service layer access via persistent Python bridge",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist"
  },
  "keywords": ["openclaw", "memory-palace", "plugin", "tools"],
  "license": "MIT",
  "dependencies": {
    "@sinclair/typebox": "^0.32.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "rimraf": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "openclaw": {
    "extensions": ["./dist/index.js"]
  }
}
